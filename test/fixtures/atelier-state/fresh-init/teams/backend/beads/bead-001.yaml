id: bead-001
title: "Add input validation to user creation endpoint"
description: "The POST /api/users endpoint accepts any payload without validation. Add schema validation for required fields (name, email) and return 400 on invalid input."
team: backend
priority: medium
status: open
assigned_to: null
depends_on: []
blocked_by: []
acceptance_criteria:
  - "POST /api/users returns 400 if name is missing"
  - "POST /api/users returns 400 if email is invalid"
  - "Valid requests still return 201"
  - "Validation errors include descriptive messages"
skill_targets:
  - testing
  - debugging
type: feature
files:
  - src/routes/users.ts
hints:
  - "Look at express-validator or zod for schema validation"
created_at: "2026-03-31T09:00:00.000Z"
completed_at: null

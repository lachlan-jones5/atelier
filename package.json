{
  "name": "atelier-dev",
  "version": "0.1.0",
  "description": "A craftsperson's workshop for software engineers — MCP server for Claude Code",
  "type": "module",
  "bin": {
    "atelier": "./bin/atelier.ts"
  },
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "zod": "^3.23.0",
    "yaml": "^2.7.0",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
